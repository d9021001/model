# ============================================================
#  Do NOT publish: raw device logs & paper-2 dataset bundle
# ============================================================
*.csv
*.zip
work_app_time_num_ai/
app_usage_time_freq_dataset/

# ============================================================
#  Do NOT publish: the manuscripts (two papers) + front matter
#  and any manuscript-derived text dumps
# ============================================================
*.docx
appendix_dump*.txt
math_dump.txt

# ============================================================
#  Python caches / scratch
# ============================================================
__pycache__/
*.pyc
*.pyo
*.output

# Note: feature matrices (tr*/ts*.xlsx) and pretrained weights
# (triplet.h5, pca_model.pkl) ARE published — they are derived
# features/models, not raw participant logs.
